# RLM: Recursive Language Models

When a task requires processing more content than fits in your context window,
or when you need to analyze an entire codebase/directory/document, use the RLM pattern.

See prompts/rlm-agent.md for full instructions.

Trigger: /rlm or when you detect you can't fit the input in context.
